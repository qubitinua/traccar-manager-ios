# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TraccarManager' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TraccarManager
  pod 'SwiftyJSON', '~> 4.0'
  pod 'SocketRocket'
  pod 'Alamofire', '~> 5.4'

  target 'TraccarManagerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TraccarManagerUITests' do
    # Pods for testing
  end

end
